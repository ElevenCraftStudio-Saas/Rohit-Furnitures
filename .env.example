# Public site URL — used for metadata, sitemap, robots, JSON-LD, OG image.
NEXT_PUBLIC_SITE_URL=https://rohitfurnitures.example.com

# ── Optional: email the contact form to the owner via Resend ──
# Only needed if you switch the contact form from WhatsApp to email
# (see app/api/contact/route.ts and components/sections/contact-form.tsx).
# RESEND_API_KEY=re_xxxxxxxx
# CONTACT_TO_EMAIL=owner@example.com
